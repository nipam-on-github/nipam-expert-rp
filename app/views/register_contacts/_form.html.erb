  <div class="form-row">
    <div class="form-group col-sm-3">
      <%= image_tag("covid19-5.png", alt: 'covid-19', id: "covid19") %>
    </div>

    <div class="form-group col-sm-3 col-sm-offset-6">
      <%= image_tag("covid19-5.png", alt: 'covid-19', id: "covid19") %>
    </div>
  </div>


<%= form_with(model: register_contact, local: true) do |form| %>
  <% if register_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(register_contact.errors.count, "error") %> prohibited this register from being saved:</h2>

      <ul>
      <% register_contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h4>Required fields are followed by <abbr title="Required">*</abbr></h4>
  
  <div class="form-row">
    <div class="form-group col-sm-12">
      <h4><%= form.label 'Contact Number' %> <abbr title="Required" aria-label="required">*</abbr></h4>
      <%= form.number_field :number, class: "form-control" %>
    </div>
  </div>

  <div class="form-row">
    <div class="actions form-group col-sm-4 col-sm-offset-4">
      <%= form.submit 'Submit', class: 'btn btn-primary', id: "register-submit" %> <hr />
    </div>
  </div>

<% end %>
