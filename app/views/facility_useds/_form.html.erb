<%= form_with(model: facility_used, local: true) do |form| %>
  <% if facility_used.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facility_used.errors.count, "error") %> prohibited this facility_used from being saved:</h2>

      <ul>
      <% facility_used.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :facility %>
    <%= form.text_field :facility %>
  </div>

  <div class="field">
    <%= form.label :visit_id %>
    <%= form.text_field :visit_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
